apiVersion: v1
kind: Service
metadata:
  name: officyna-service
  namespace: officyna
  labels:
    app.kubernetes.io/name: officyna
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: officyna
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP